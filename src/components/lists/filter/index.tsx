import React, { FC } from "react";
import styles from "./filter.module.scss";

const FilterList: FC = () => {
  return <div className={styles.filterBlock}>Фильтрация</div>;
};

export default FilterList;
